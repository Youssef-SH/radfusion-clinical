config_version: 1
name: medsiglip_placeholder
executable: false

dataset:
  registry_key: rsna
  manifest_directory: data/manifests
  task_id: pneumonia

model:
  registry_key: medsiglip
  output_name: medsiglip
  modality: image
  class_weighting: configured
  parameters:
    encoder_name: medsiglip
    image_size: 512
  fit_parameters: {}

training:
  seed: 42
  report_directory: reports
  model_directory: models/rsna
  require_clean_git: true

evaluation:
  threshold_policy: youden_j
  sensitivity_target: 0.90
  calibration_bins: 15
  latency_warmup_calls: 100
  latency_measured_calls: 1000

mlflow:
  experiment_name: radfusion-rsna
  tracking_directory: mlruns
